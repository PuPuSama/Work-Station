# Lab 03：玩具向量与余弦相似度

## 解锁条件

Lab 02 通过后再开始。

## 目标

使用手工提供的小向量理解点积、向量长度和余弦相似度。这里不调用 Embedding API，因为当前目标是看懂排序依据，而不是记住 SDK 写法。

实现：

- `dot_product()`
- `vector_norm()`
- `cosine_similarity()`

不能使用 NumPy。

