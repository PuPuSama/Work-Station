# Lab 04：Top-K 检索与元数据过滤

## 解锁条件

Lab 03 通过后再开始。

## 目标

把余弦相似度用于一个微型产品检索器，并观察纯语义相似为什么不足以区分 Product 和 Blog。

`top_k_search()` 必须：

- 先按允许页面类型和精确分类过滤。
- 计算剩余候选的相似度。
- 按分数降序返回前 K 条。
- 分数相同时按 `document_id` 排序，保证结果可重复。
- 拒绝小于等于 0 的 K。

