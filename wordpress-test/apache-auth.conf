# Apache otherwise keeps the Basic Authorization header out of PHP.  This is
# required for the WordPress Application Password used by the disposable test
# site; it is never mounted by the Article Agent production image.
SetEnvIf Authorization "(.*)" HTTP_AUTHORIZATION=$1
